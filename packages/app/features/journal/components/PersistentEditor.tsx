/**
 * Web stub for PersistentEditor.
 * On web, we don't need a persistent editor - WebView initialization isn't an issue.
 * Returns null to avoid rendering anything.
 */
export const PersistentEditor = () => null
