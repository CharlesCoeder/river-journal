import { YStack, Text } from '@my/ui'

// Placeholder — tab press is intercepted to open full-screen journal
export default function WriteTab() {
  return (
    <YStack flex={1} justifyContent="center" alignItems="center">
      <Text>Loading...</Text>
    </YStack>
  )
}
